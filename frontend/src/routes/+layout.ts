// Render as a client-side SPA: no server-side rendering, with an index.html
// fallback produced by adapter-static. Suits a single API-driven page.
export const ssr = false;
export const prerender = false;
